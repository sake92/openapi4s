openapi: 3.0.1
info: {title: t, version: "1"}
paths:
  /hello:
    get:
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UuidAlias'
components:
  schemas:
    UuidAlias:
      type: string
      format: uuid
    Thing:
      type: object
      properties:
        id:
          $ref: '#/components/schemas/UuidAlias'
        status:
          type: string
          enum: ["open", null]
